#include<bits/stdc++.h>
using namespace std;
// Function are set of code which perform something for you
//  Function are used to modularies code
// Function are used to increase readability
// Function are used to use same cod emultiple times
// void f -> which does not returns anything
// return f -> 
// parameaters
// non parameterised 

void printName(string name) {          // which is does returns anything
    cout<< "hey " << name << endl;
}


int main() {

    string name;
    cin >> name;
    printName(name);
    
    // reusebility of code

    string name2;
    cin >> name2;
    printName(name2);

    string m;
    cin >> m;
    printName(m);

    string myname;
    cin>> myname;
    printName(myname);
    return 0;

  
}

// () - non parameater