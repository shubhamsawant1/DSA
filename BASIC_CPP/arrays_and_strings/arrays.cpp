#include<iostream>
using namespace std;

int main () {

    // int a, b, c, d;
    // cin >> a >> b >> c >> d;
    int arr[5];
    cin >> arr[0] >> arr[1] >> arr[2] >> arr[3] >> arr[4];
    // if you want to add yes you can add
    arr[3] += 10;
    arr[3] = 5; // change your value with 5
    cout << arr[3];



    return 0;

} 